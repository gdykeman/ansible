---
- name: Check Cisco IOS running Version
  hosts: ios

  tasks:
    - name: collect and validate Version
      ios_command:
        commands: show Version
        waitfor: result[0] contains "Version 15.6(1)T"
        provider: "{{ cli }}"